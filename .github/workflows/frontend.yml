name: Frontend CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'Frontend/**'
      - '.github/workflows/frontend.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./Frontend
          file: ./Frontend/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/webresep-frontend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/webresep-frontend:${{ github.sha }}

      - name: Update Kubernetes Manifest
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Clone k8s repo
          git clone https://${{ secrets.GH_PAT }}@github.com/GinTrmksh/webresep-k8s.git
          cd webresep-k8s
          
          # Update image tag
          sed -i "s|image: .*/webresep-frontend:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/webresep-frontend:${{ github.sha }}|g" k8s/frontend-deployment.yaml
          
          # Commit and push
          git add k8s/frontend-deployment.yaml
          git commit -m "Update frontend image to ${{ github.sha }}"
          git push